# 第十六题：字典数据查找（思考题）
# 问题：在字典中查找仅限于关键字查找，为什么Python语言不提供
#       在字典条目的数据值中查找的功能呢？有什么不便之处吗？

print("""
【答案】

一、Python 不提供值查找的设计原因：

1. 性能考虑（核心原因）
   - 字典底层是哈希表，键查找通过哈希函数直接定位，时间复杂度 O(1)
   - 值没有建立哈希索引，查找值只能遍历所有条目，时间复杂度 O(n)
   - 如果支持值查找，会破坏字典"高效查找"的设计初衷

2. 值可能不唯一
   - 多个键可能对应相同的值，如 {'a': 1, 'b': 1}
   - 反向查找会返回多个结果，无法像键查找那样确定唯一值

3. 值可能是可变类型
   - 字典的值可以是列表、字典等可变类型
   - 可变类型不可哈希，无法建立反向索引

二、不便之处：

1. 反向查找（按值找键）需要手动遍历，代码较繁琐
2. 频繁的反向查找场景下效率较低

三、解决方案（用代码演示）：
""")

# 演示：手动按值查找键
student = {'Tom': 18, 'Jack': 20, 'Lucy': 18}
print(f"字典: {student}")

# 场景：查找年龄为18岁的同学（按值找键）
target_value = 18
print(f"\n查找值 = {target_value} 的所有键:")
keys = [k for k, v in student.items() if v == target_value]
print(f"  结果: {keys}")
print("  说明: 值不唯一时，反向查找会返回多个键")

# 场景：如果需要频繁反向查找，可以构建反转字典
print("\n构建反转字典（适合值唯一的场景）:")
reversed_dict = {v: k for k, v in student.items()}
print(f"  反转字典: {reversed_dict}")
print("  注意: 值不唯一时，后面的键会覆盖前面的键")
